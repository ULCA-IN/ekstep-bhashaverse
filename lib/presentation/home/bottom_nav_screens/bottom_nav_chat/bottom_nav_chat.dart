import 'package:flutter/material.dart';

class BottomNavChat extends StatelessWidget {
  const BottomNavChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Chat Screen will appear here'),
    );
  }
}
